<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/menu_search_contacts"
        android:icon="@drawable/search_icon"
        android:title="@string/contact_picker_menu_item_search_contacts_title"
        app:actionViewClass="androidx.appcompat.widget.SearchView"
        app:showAsAction="ifRoom|collapseActionView" />

    <item
        android:id="@+id/menu_refresh_contacts"
        android:icon="@drawable/sync_icon"
        app:showAsAction="ifRoom"
        android:title="@string/contact_picker_menu_item_refresh_contacts_title" />

</menu>